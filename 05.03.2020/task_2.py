"""
Create network: classes with required functions
(Authorization(Registration)), add password validation(contains
symbols and numbers), unique login.
User can sign up(login, password, repeat password) -> user sign in.
User can logout, sign in as another user.
Class User(1 - admin, 2 - common user),
Common User - can post(with date),
Admin - can view all users, they registration date and posts.
"""